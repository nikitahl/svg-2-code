{
  "name": "svg-2-code",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "git@github.com:nikitahl/svg-2-code.git",
  "author": "Nikita Hlopov",
  "license": "MIT",
  "scripts": {
    "start": "parcel index.html",
    "predeploy": "rm -rf dist && parcel build index.html --public-url ./",
    "deploy": "gh-pages -d dist",
    "lint": "yarn eslint . --ignore-pattern node_modules --ignore-pattern dist --ignore-pattern .cache --ignore-pattern codemirror"
  },
  "dependencies": {
    "eslint": "^9.21.0",
    "gh-pages": "^6.3.0",
    "parcel-bundler": "^1.12.5"
  },
  "devDependencies": {
    "parcel": "^2.13.3",
    "parcel-plugin-meta-images": "github:plumdumpling/parcel-plugin-meta-images"
  }
}
